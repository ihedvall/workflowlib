# Workflow Library and Applications
The project consist of a library that define task and workflow. The task is actually written in Python, C++ or Lua, so this library is interface to these languages 
